{% extends 'base.html.twig' %}

{% block body %}
    <h1>Subtype</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Name</th>
                <td>{{ subtype.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('subtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('subtype_edit', { 'id': subtype.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
