{% extends 'base.html.twig' %}

{% block body %}
<center>

    <h1>Movies list</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Name</th>
                <th>Released</th>
            </tr>
        </thead>
        <tbody>
        {% for movie in movies %}
            <tr>
                <td>{{ movie.name }}</td>
                <td>{% if movie.released %}{{ movie.released|date('Y-m-d') }}{% endif %}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('movie_detail', { 'id': movie.id }) }}">More informations</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

</center>
{% endblock %}
